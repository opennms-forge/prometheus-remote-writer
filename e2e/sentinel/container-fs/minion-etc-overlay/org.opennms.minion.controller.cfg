# Minion controller configuration. Identifies this Minion to Core via the
# OpenNMS REST API and binds it to a location.
id           = minion-sentinel-poc
location     = Default
broker-url   =
http-url     =
