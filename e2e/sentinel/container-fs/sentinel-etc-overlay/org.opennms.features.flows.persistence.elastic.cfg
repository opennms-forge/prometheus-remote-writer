# Sentinel-flows defaults to writing to Elasticsearch. Even if we don't
# enable sentinel-flows in featuresBoot, having a working ES URL keeps
# the default-boot machinery happy if any transitive feature pulls it in.
elasticUrl = http://elasticsearch:9200
