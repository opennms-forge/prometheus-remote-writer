# Common Kafka IPC fallback. Read by RPC server (rpc.kafka) and Twin
# subscriber (twin.kafka) blueprints when their specific PIDs don't carry
# bootstrap.servers. Defensive: the specific .cfg files in this overlay
# already supply the broker, but having this here means a future operator
# editing one of them won't accidentally break the other paths.
bootstrap.servers = kafka:9092
