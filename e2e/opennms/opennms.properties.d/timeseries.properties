# Activate the integration-api TSS layer so Horizon routes sample collection
# through the prometheus-remote-writer plugin registered as a
# TimeSeriesStorage service.
org.opennms.timeseries.strategy=integration
